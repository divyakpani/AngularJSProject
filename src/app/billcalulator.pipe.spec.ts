import { BillcalulatorPipe } from './billcalulator.pipe';

describe('BillcalulatorPipe', () => {
  it('create an instance', () => {
    const pipe = new BillcalulatorPipe();
    expect(pipe).toBeTruthy();
  });
});
